# ============================================================
# Hyprland - window/layer rules.
# Part of the "Avalon Amber" rice. Managed via chezmoi.
# ============================================================

# Power menu: keep it a small centered window instead of a fullscreen overlay
windowrule = match:class ^(wlogout)$, float 1, center 1, suppress_event fullscreen
